apply plugin: 'java'
version = '2.0'

task zip(type: Zip) {
    from 'src/dist'
    from configurations.runtime
}

repositories {
	maven {
     url "http://scheme2ddl.googlecode.com/svn/m2/releases"
	 }
}

dependencies {
    runtime 'com.googlecode:scheme2ddl:2.0'
}
